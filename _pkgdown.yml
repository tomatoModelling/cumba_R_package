# Minimal safe config (no favicon, no URL, no extras)
template:
  bootstrap: 5
  bootswatch: flatly

home:
  title: "CUMBA"
  description: "Daily tomato model for yield & Brix"

reference:
  - title: "Functions"
    contents:
      - cumba_experiment
